{% extends '@App/main.html.twig' %}

{% block body %} 
    {{form_start(add_form)}}
        {{form_row(add_form.name, {'label': 'Имя'})}}
        {{form_row(add_form.position, {'label': 'Должность'})}}
    {{form_end(add_form)}}
{% endblock %}

{% block stylesheets %}

{% endblock %}
