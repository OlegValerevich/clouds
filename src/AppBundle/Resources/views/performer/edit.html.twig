{% extends '@App/main.html.twig' %}

{% block body %} 
    {{form_start(edit_form)}}
        {{form_row(edit_form.name, {'label': 'Имя'} ) }}
        {{form_row(edit_form.position, {'label': 'Должность'} ) }}
    {{form_end(edit_form)}}
{% endblock %}

{% block stylesheets %}{% endblock %}
