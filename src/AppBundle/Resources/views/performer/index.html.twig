{% extends '@App/main.html.twig' %}

{% block body %} 
        <table class="table table-sm">
            <thead>
              <tr>
                <th scope="col">id</th>
                <th scope="col">Имя</th>
                <th scope="col">Должность</th>
                <th scope="col">Действие</th>
              </tr>
            </thead>
            <tbody>
            {% for performer in performers %}   
              <tr>
                <td scope="row" >{{ performer.id }}</td>
                <td>{{ performer.name }}</td>
                <td>{{ performer.position }}</td>
                <td>
                    <a id="" href="" onclick="removePerformer({{ performer.id }});">Удалить</a>
                    /
                    <a href="" id="">Редактировать</a>
                </td>
              </tr>
            {% endfor %}
            </tbody>
        </table>
        <a class="btn btn-outline-success my-2 my-sm-0" href="{{ path('add_performer') }}">Добавить</a>

{% endblock %}

{% block stylesheets %}

{% endblock %}
